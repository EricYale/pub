# Frame Rate
- If our machine runs under the framerate, we can call update multiple times per frame to bring the game state up to speed
- If our machine runs over the framerate, we can employ frame-capping to make sure everything animates at the correct speed
