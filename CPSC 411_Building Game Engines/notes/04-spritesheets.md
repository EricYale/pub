# Sprite Sheets
- To save resources, multiple sprites can be loaded into a spritesheet
- Pre-defined bounds of each sprite are defined (and possibly loaded from a config file like JSON)
- Sprite sheets can also be used for bitmap fonts
