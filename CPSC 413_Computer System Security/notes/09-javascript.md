# Javascript

## What is JS?
- ECMAScript specification
- Allows controlling the DOM

## Properties of JS
- Dynamic typing
- Primitives and objects
    - All objects have a prototype
    - All objects are instances of functions
- Variables
    - `var` keyword is a variable that is restricted to the scope of the function; `for`, `if`, etc. do not create a new scope
